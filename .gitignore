# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.ipynb_checkpoints/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Environment
.env
.env.*
!.env.example

# Data & artifacts (reproducible via `make data` / training)
data/raw/
data/processed/
*.duckdb
*.parquet
mlruns/
.mlflow/

# ...but committed derived geometry assets are kept (small, reproducible via
# scripts/build_zone_geometry.py)
!src/ridepulse/sim/core/data/
!src/ridepulse/sim/core/data/*.parquet
!src/ridepulse/sim/core/data/*.csv

# Node (Claude Code is installed globally, not vendored)
node_modules/

# OS
.DS_Store
